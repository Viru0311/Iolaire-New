# Render Blueprint - optional; you can also configure everything in the Dashboard.
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: iolaire
    runtime: node
    region: oregon  # or singapore / frankfurt

    buildCommand: npm install && npm run build
    startCommand: npm start

    envVars:
      - key: NODE_ENV
        value: production

    # Uncomment and set if you add a database later:
    # envVars:
    #   - key: DATABASE_URL
    #     fromDatabase:
    #       name: iolaire-db
    #       property: connectionString
